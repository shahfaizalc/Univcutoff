<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.faikan.univcounselling">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat">

        <activity
            android:name="com.faikan.univcounselling.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.faikan.univcounselling.MainActivity"
            android:label="@string/app_name" />

        <activity
            android:name="com.faikan.univcounselling.FilterActivity"
            android:label="Filter Options"
            android:parentActivityName="com.faikan.univcounselling.MainActivity" />
        <activity
            android:name="com.faikan.univcounselling.ImportantDatesActivity"
            android:label="Important Dates"

            android:configChanges="orientation|screenSize" />

        <!-- Home Activity -->
        <activity
            android:name="com.faikan.univcounselling.HomeActivity" />
    </application>

</manifest>
